<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sh334379.hostelmanagement">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".RegistrationActivity" />
        <activity android:name=".PaymentHsitoryActivity" />
        <activity android:name=".PaymentActivity" />
        <activity android:name=".RoomStatusActivity" />
        <activity android:name=".ChatActivity" />
        <activity android:name=".ContactActivity" />
        <activity android:name=".PaymentSuccessActivity" />
        <activity android:name=".MonthlyPaymentViewActivity" />
        <activity android:name=".DetailedMonthlyPaymentInfoActivity" />
        <activity android:name=".FloorViewActivity" />
        <activity android:name=".RoomViewActivity" />
        <activity android:name=".DashboardActivity"></activity>
    </application>

</manifest>